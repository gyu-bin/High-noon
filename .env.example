# ElevenLabs (local only — never commit .env)
ELEVENLABS_API_KEY=

# High Noon PvP (Supabase) — copy to .env
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# 운영 대시보드 PIN — Supabase app_settings.admin_pin 과 동일해야 함
# insert into app_settings (key, value) values ('admin_pin', 'your-pin');
